/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        remotePatterns: [
          {
            hostname: 'akmweb.youngjoygame.com',
          },
        ],
      }, 
};

export default nextConfig;
